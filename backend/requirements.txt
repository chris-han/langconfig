# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.31.0
pydantic>=2.10.0
pydantic-settings>=2.10.1
python-multipart

# Database
sqlalchemy==2.0.35
alembic==1.13.3
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0

# LangChain v1.0 + LangGraph (latest versions)
langchain>=1.2.7
langchain-core>=1.2.7
langchain-openai>=1.0.1
langchain-anthropic>=1.2.0
langchain-google-genai>=4.1.2
langchain-community==0.4.1  # Pinned version for stability
langchain-postgres
langgraph>=1.0.7
langgraph-checkpoint-postgres
# Multi-agent and dynamic tool libraries
langgraph-supervisor
langgraph-swarm
langgraph-bigtool
tiktoken>=0.5.0
langchain-text-splitters  # Advanced text splitting capabilities

# LlamaIndex + Vector Stores
llama-index
llama-index-vector-stores-postgres
llama-index-embeddings-azure-openai
llama-index-embeddings-openai
pgvector
numpy

# Document Processing
# Using LangChain Community loaders instead of direct libraries
unstructured[all-docs]>=0.10.0  # Universal document loader (PDF, DOCX, etc.) with OCR
pypdf>=4.0.0  # Fallback PDF loader when unstructured fails
python-docx>=1.1.0  # Word document generation for export functionality
# Note: PyPDF2 and python-docx still available as fallbacks via langchain-community
beautifulsoup4>=4.12.0
python-magic-bin>=0.4.14; platform_system=="Windows"  # File type detection on Windows

# MCP Protocol Support
fastapi-mcp>=0.1.0

# DeepAgents - Agent harness (requires Python >=3.11)
deepagents>=0.2.4

# Real-time Monitoring (SSE for desktop app streaming)
sse-starlette>=2.1.3

# Note: Redis NOT needed for desktop app
# Event streaming handled by in-memory asyncio.Queue (services/event_bus.py)
# Redis only needed if deploying as multi-instance cloud service

# Browser Automation
playwright>=1.48.0  # For browser tool (requires: python -m playwright install chromium)

# Security & Utilities
cryptography
python-dotenv==1.0.1
httpx>=0.28.1
aiofiles==24.1.0
python-json-logger
deepdiff>=7.0.0  # Phase 2: Deep comparison for workflow version diffs

# Scheduling
croniter>=2.0.0  # Cron expression parsing and next run calculation
pytz>=2024.1  # Timezone support for scheduled workflows

# File System Monitoring (optional - for file watch triggers)
watchdog>=4.0.0  # Cross-platform file system monitoring

# Presentation Generation
google-api-python-client>=2.100.0  # Google Slides API
google-auth>=2.23.0  # Google OAuth
google-auth-oauthlib>=1.0.0  # OAuth flow helpers
python-pptx>=0.6.21  # PowerPoint/PDF generation

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
asyncpg
