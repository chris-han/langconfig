# =============================================================================
# LangConfig Backend Environment Variables
# =============================================================================
# Copy this file to .env in the backend directory
# DO NOT commit .env to version control - it contains sensitive credentials

# =============================================================================
# Environment
# =============================================================================
ENVIRONMENT=development  # Options: development, production, testing
DEBUG=true  # Set to false in production for less verbose logging

# =============================================================================
# Rate Limiting
# =============================================================================
RATE_LIMIT_REQUESTS_PER_MINUTE=300

# =============================================================================
# External API Keys (for tools)
# =============================================================================
# OpenAI API key (required for OpenAI cloud embeddings path)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Azure OpenAI embeddings path (optional alternative to OPENAI_API_KEY)
AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_VERSION=2024-05-01-preview
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=your-embedding-deployment-name
AZURE_OPENAI_EMBEDDING_DIMENSIONS=1536

# Embedding model used by the vector index
EMBEDDING_MODEL=text-embedding-3-small

# Tavily Search API (optional - for web search tools)
TAVILY_API_KEY=tvly-your-tavily-api-key-here

# =============================================================================
# Google OAuth (for Presentation Generator - Google Slides export)
# =============================================================================
# Required for exporting presentations to Google Slides
# See docs/GOOGLE_OAUTH_SETUP.md for setup instructions
#
# 1. Create OAuth credentials at https://console.cloud.google.com/
# 2. Enable Google Slides API and Google Drive API
# 3. Create OAuth 2.0 Client ID (Web application)
# 4. Add redirect URI: http://localhost:8765/api/auth/google/callback
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-google-client-secret

# =============================================================================
# Encryption Key (auto-generated on first run)
# =============================================================================
# The encryption key is stored in backend/encryption.key
# It's auto-generated if missing - no need to set manually
# NEVER commit this key or share it
